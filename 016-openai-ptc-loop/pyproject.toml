[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openai-ptc-loop-sample"
version = "0.1.0"
description = "Offline fixture for a stateless Programmatic Tool Calling loop"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
